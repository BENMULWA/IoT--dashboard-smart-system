// postcss.config.cjs that is used by Tailwind CSS to process your CSS files. It includes the Tailwind CSS plugin and the Autoprefixer plugin, which adds vendor prefixes to your CSS for better browser compatibility.

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}